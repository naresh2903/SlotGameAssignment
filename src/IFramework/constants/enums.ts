export enum Ways {
    LTR = "leftToRight", // Left to Right
    RTL = "rightToLeft", // Right to Left
    BOTH = "both" // LTR + RTL
}

export enum ScreenTypes {
    ROWxCOLUMN = "rowToColumn", // Row to Column Screen => Arrays are formed row wise
    COLUMNxROW = "columnToRow" // Column to Row Screen => Arrays are formed reel Wise
}